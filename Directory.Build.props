<Project>
  <PropertyGroup>
    <!-- Opciones del lenguaje aplicadas a todos los .csproj -->
    <TargetFramework>net7.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AnalysisLevel>latest</AnalysisLevel>
  </PropertyGroup>
</Project>